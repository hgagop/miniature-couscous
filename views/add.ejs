<%- include('./partials/header') %>

<div class="ui main text container segment">
    <div class="ui huge header">Add New Item</div>
    <form action="/inventory" method="post" class="ui form">
        <div class="field">
            <label>Name</label>
            <input type="text" name="wine[name]" placeholder="name" required>
        </div>
        <div class="field">
            <label>Location</label>
            <input type="text" name="wine[location]" placeholder="location" required>
        </div>
        <div class="field">
            <label>Quantity</label>
            <input type="number" name="wine[quantity]" placeholder="quantity" required>
        </div>
        <div class="field">
            <label>Type</label>
            <input type="text" name="wine[type]" placeholder="type" required>
        </div>
        <input class="ui violet basic button" type="submit">
    </form>
</div>

<%- include('./partials/footer') %> 